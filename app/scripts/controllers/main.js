'use strict';

/**
 * @ngdoc function
 * @name timestampConverterApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the timestampConverterApp
 */
angular.module('timestampConverterApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
